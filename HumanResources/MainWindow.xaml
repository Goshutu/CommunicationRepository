<Window x:Class="HumanResources.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HumanResources"
        mc:Ignorable="d"
        Title="MainWindow" Height="150" Width="210" ResizeMode="NoResize">
    <GroupBox Margin="5">
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="34" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Companies list:" VerticalAlignment="Bottom" Margin="5" />
            <Button Grid.Row="0" Grid.Column="1" Content="Open" Click="OpenCompaniesButton_Click"  MinWidth="60" Margin="5" />
        </Grid>
    </GroupBox>
</Window>
